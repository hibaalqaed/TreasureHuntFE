// React
import { Route, Switch } from "react-router";
import { observer } from "mobx-react";

// Components

const Routes = () => {
  return (
    <Switch>
      <Route></Route>
    </Switch>
  );
};

export default observer(Routes);
